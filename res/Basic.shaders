#shader vertex
#version 130
in vec4 position;

void main()
{
	gl_Position = vec4(position.x, position.y, position.z, 1.0);
};


#shader fragment
#version 130

void main()
{
	gl_FragColor = vec4(1.0f, 0.0f, 0.0f, 1.0f);
};